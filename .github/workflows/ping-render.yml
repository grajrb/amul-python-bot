name: Ping Render
on:
  schedule:
    - cron: "*/5 * * * *"  
  workflow_dispatch:    
jobs:  
  ping:
    runs-on: ubuntu-latest  
    steps:    
      - name: Ping Render service    
        run: curl -s https://amul-python-bot-jhj1.onrender.com/health || true      
